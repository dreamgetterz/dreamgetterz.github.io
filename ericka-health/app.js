
/* Local-only UI. No external script, analytics, credentials, or automated medical decisions. */
(function(){
'use strict';
const C=window.ErickaCore,$=id=>document.getElementById(id),$$=q=>Array.from(document.querySelectorAll(q));
const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const text=(id,v)=>{$(id).textContent=v;};const DAYS=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];
let state=C.defaults(),selectedDate=C.today(),activeScreen='home',storageOK=false,storageProblem='',rawCache=null,toastTimer,undoAction=null,currentDay=C.today();
const uid=()=>globalThis.crypto?.randomUUID?.()||'r-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2);
const pretty=n=>n===null?'—':String(C.round(n));
const KNOWLEDGE=[{"id":"tirzepatide","name":"Tirzepatide","aliases":["tirzepatide","tirzep","trizepatide","zepbound","mounjaro","glp2"],"category":"Weight & metabolic health","level":"Approved product labeling","body":"Tirzepatide activates GIP and GLP-1 receptors. Zepbound is approved for weight management in eligible adults. Its 2.5 mg weekly dose is an initiation dose, not an approved maintenance dose. The tracker never changes your dose automatically.","risk":"Nausea, vomiting, diarrhea and constipation can occur. The label includes important thyroid-tumor, pancreatitis, dehydration and other warnings. Read the full product-specific instructions with your prescriber. Research-vial concentrations are not interchangeable with branded presentations.","url":"https://pi.lilly.com/us/zepbound-uspi.pdf","source":"Lilly · Zepbound prescribing information","reviewed":"2026-09-19"},{"id":"bpc157","name":"BPC-157","aliases":["bpc157","bpc"],"category":"Research peptides","level":"Limited human safety evidence","body":"BPC-157 appears in FDA’s compounding-safety review. This library does not establish a human recovery benefit.","risk":"FDA flags immune-response and impurity concerns; adequate safety evidence is lacking.","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks","source":"FDA · Compounding safety review","reviewed":"2026-09-19"},{"id":"tb500","name":"TB-500","aliases":["tb500","thymosinbeta4"],"category":"Research peptides","level":"Important identity distinction","body":"FDA’s TB-500 entry refers to the LKKTETQ fragment of thymosin beta-4, not every thymosin beta-4 product.","risk":"FDA reports no identified human-exposure data for that fragment.","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks","source":"FDA · Compounding safety review","reviewed":"2026-09-19"},{"id":"motsc","name":"MOTS-C","aliases":["motsc","mots"],"category":"Metabolic research","level":"Preclinical research","body":"MOTS-C is a mitochondria-derived peptide investigated in metabolic signaling. The original study included cell and mouse experiments; these do not establish an effective human weight-loss treatment.","risk":"An animal result is not a human dosing or safety guide.","url":"https://pubmed.ncbi.nlm.nih.gov/25738459/","source":"Lee et al. · Cell Metabolism, 2015","reviewed":"2026-09-19"},{"id":"ghkcu","name":"GHK-Cu","aliases":["ghkcu","ghk","copperpeptide"],"category":"Research peptides","level":"Route matters","body":"FDA’s safety entry concerns injectable GHK-Cu. It is not a blanket assessment of topical cosmetics.","risk":"Human safety information is limited; aggregation and impurities may cause immune responses.","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks","source":"FDA · Injectable GHK-Cu review","reviewed":"2026-09-19"},{"id":"ss31","name":"SS-31 / elamipretide","aliases":["ss31","elamipretide","forzinity"],"category":"Mitochondrial medicine","level":"Indication-specific FDA approval","body":"Elamipretide is also known as SS-31. FDA granted accelerated approval to Forzinity in September 2025 for improving muscle strength in people with Barth syndrome weighing at least 30 kg.","risk":"That specific approval does not establish general anti-aging, energy, or weight-loss benefits, or the quality of online research vials.","url":"https://www.fda.gov/news-events/press-announcements/fda-grants-accelerated-approval-first-treatment-barth-syndrome","source":"FDA · Forzinity approval","reviewed":"2026-09-19"},{"id":"kpv","name":"KPV","aliases":["kpv"],"category":"Research peptides","level":"Evidence gap","body":"KPV is included in FDA’s review of compounding safety.","risk":"FDA has not identified human-exposure data; human risks remain insufficiently characterized.","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks","source":"FDA · Compounding safety review","reviewed":"2026-09-19"},{"id":"ipamorelin","name":"Ipamorelin","aliases":["ipamorelin","ipa"],"category":"Research peptides","level":"Route-specific safety concerns","body":"FDA reviews ipamorelin acetate separately from approved medicines.","risk":"Serious events were reported with intravenous use; other injectable routes lack adequate safety information.","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks","source":"FDA · Compounding safety review","reviewed":"2026-09-19"},{"id":"cjc1295","name":"CJC-1295","aliases":["cjc1295","cjc"],"category":"Research peptides","level":"Limited clinical data","body":"CJC-1295 is included in FDA’s safety review.","risk":"Reported concerns include increased heart rate and systemic vasodilation.","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks","source":"FDA · Compounding safety review","reviewed":"2026-09-19"},{"id":"wolverine","name":"Wolverine / blend names","aliases":["wolverine","glow","klow","stack"],"category":"Understanding labels","level":"Not a verified protocol","body":"A blend nickname does not tell this app what is actually in a vial. Record each ingredient and the exact product label; do not infer a dose from a nickname.","risk":"This library does not recommend starting, combining, or sequencing research peptides.","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks","source":"FDA · Ingredient-specific safety review","reviewed":"2026-09-19"},{"id":"pace","name":"Healthy weight-loss reference lines","aliases":["healthyweight","weightloss","reference","traditional","healthy"],"category":"Health basics","level":"General guidance","body":"CDC describes about 1–2 lb per week as a gradual pace. Your two chart lines are illustrations, not a forecast or a personalized safety assessment. Weight is only one part of health.","risk":"This app cannot determine whether a chosen goal or weight-loss pace is medically appropriate for you.","url":"https://www.cdc.gov/healthy-weight-growth/losing-weight/index.html","source":"CDC · Steps for Losing Weight","reviewed":"2026-09-19"},{"id":"habits","name":"More than the scale","aliases":["health","sleep","habits","movement","nutrition"],"category":"Health basics","level":"General guidance","body":"Eating patterns, physical activity, sleep, and stress management all belong in healthy weight management. Choose a few realistic, specific steps rather than treating the scale as a health score.","risk":"No universal calorie or protein target is assigned by this app.","url":"https://www.cdc.gov/healthy-weight-growth/losing-weight/index.html","source":"CDC · Steps for Losing Weight","reviewed":"2026-09-19"},{"id":"servings","name":"Food labels & serving sizes","aliases":["servingsize","foodlabel","nutritionlabel"],"category":"Food tracking","level":"Label-reading basics","body":"Nutrition on a food label is usually listed per serving. Enter the number of servings eaten; the diary multiplies each entered nutrient. A serving size is not a personal recommendation.","risk":"Unknown calories or macros stay unknown. A plus sign indicates a partial daily total.","url":"https://www.fda.gov/food/nutrition-facts-label/how-understand-and-use-nutrition-facts-label","source":"FDA · Nutrition Facts label","reviewed":"2026-09-19"}];
const normalize=q=>String(q).toLowerCase().replace(/[^a-z0-9]/g,'');
function findKnowledge(q){const n=normalize(q);return KNOWLEDGE.filter(k=>k.aliases.some(a=>n.includes(a)));}
const lessons=[{"title":"Tirzepatide","body":"Tirzepatide activates GIP and GLP-1 receptors. Zepbound is approved for weight management in eligible adults. Its 2.5 mg weekly dose is an initiation dose, not an approved maintenance dose. The tracker never changes your dose automatically.","source":"Lilly · Zepbound prescribing information","url":"https://pi.lilly.com/us/zepbound-uspi.pdf"},{"title":"More than the scale","body":"Eating patterns, physical activity, sleep, and stress management all belong in healthy weight management. Choose a few realistic, specific steps rather than treating the scale as a health score.","source":"CDC · Steps for Losing Weight","url":"https://www.cdc.gov/healthy-weight-growth/losing-weight/index.html"},{"title":"MOTS-C","body":"MOTS-C is a mitochondria-derived peptide investigated in metabolic signaling. The original study included cell and mouse experiments; these do not establish an effective human weight-loss treatment.","source":"Lee et al. · Cell Metabolism, 2015","url":"https://pubmed.ncbi.nlm.nih.gov/25738459/"},{"title":"Food labels & serving sizes","body":"Nutrition on a food label is usually listed per serving. Enter the number of servings eaten; the diary multiplies each entered nutrient. A serving size is not a personal recommendation.","source":"FDA · Nutrition Facts label","url":"https://www.fda.gov/food/nutrition-facts-label/how-understand-and-use-nutrition-facts-label"},{"title":"SS-31 / elamipretide","body":"Elamipretide is also known as SS-31. FDA granted accelerated approval to Forzinity in September 2025 for improving muscle strength in people with Barth syndrome weighing at least 30 kg.","source":"FDA · Forzinity approval","url":"https://www.fda.gov/news-events/press-announcements/fda-grants-accelerated-approval-first-treatment-barth-syndrome"},{"title":"Healthy weight-loss reference lines","body":"CDC describes about 1–2 lb per week as a gradual pace. Your two chart lines are illustrations, not a forecast or a personalized safety assessment. Weight is only one part of health.","source":"CDC · Steps for Losing Weight","url":"https://www.cdc.gov/healthy-weight-growth/losing-weight/index.html"},{"title":"BPC-157","body":"BPC-157 appears in FDA’s compounding-safety review. This library does not establish a human recovery benefit.","source":"FDA · Compounding safety review","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks"},{"title":"GHK-Cu","body":"FDA’s safety entry concerns injectable GHK-Cu. It is not a blanket assessment of topical cosmetics.","source":"FDA · Injectable GHK-Cu review","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks"},{"title":"TB-500","body":"FDA’s TB-500 entry refers to the LKKTETQ fragment of thymosin beta-4, not every thymosin beta-4 product.","source":"FDA · Compounding safety review","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks"},{"title":"KPV","body":"KPV is included in FDA’s review of compounding safety.","source":"FDA · Compounding safety review","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks"},{"title":"Ipamorelin","body":"FDA reviews ipamorelin acetate separately from approved medicines.","source":"FDA · Compounding safety review","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks"},{"title":"CJC-1295","body":"CJC-1295 is included in FDA’s safety review.","source":"FDA · Compounding safety review","url":"https://www.fda.gov/drugs/human-drug-compounding/certain-bulk-drug-substances-use-compounding-may-present-significant-safety-risks"}];
function featured(day=C.today()){return lessons[((Math.floor((C.ord(day)-C.ord('2026-09-14'))/7)%lessons.length)+lessons.length)%lessons.length];}
function toast(message,undo=null){clearTimeout(toastTimer);$('toast').replaceChildren(document.createTextNode(message));$('toast').hidden=false;undoAction=undo;if(undo){const b=document.createElement('button');b.id='undoBtn';b.textContent='Undo';b.onclick=()=>{try{const fn=undoAction;undoAction=null;fn?.();}catch(e){toast(e.message);}};$('toast').append(b);}toastTimer=setTimeout(()=>{$('toast').hidden=true;undoAction=null;},undo?20000:8000);}
function warn(message){storageProblem=message;$('storageAlert').hidden=!message;text('storageAlert',message);text('saveStatus',storageOK?'Saved in this browser':'Saving unavailable');}
function load(){
 try{const raw=localStorage.getItem(C.KEY);rawCache=raw;if(raw!==null){state=C.validate(JSON.parse(raw));if((state.profile.planRevision||1)<2){state.profile.goalWeight=130;state.profile.targetDate='2027-01-31';state.profile.targetDays=C.ord('2027-01-31')-C.ord(state.profile.startDate);state.profile.planRevision=2;state=C.validate(state);localStorage.setItem(C.KEY,JSON.stringify(state));rawCache=JSON.stringify(state);}}else{state=C.validate(C.defaults());localStorage.setItem(C.KEY,JSON.stringify(state));rawCache=JSON.stringify(state);}storageOK=true;warn('');}
 catch(e){storageOK=false;warn('Saved data could not be read or saved. Existing browser data has not been replaced. Edits are blocked to prevent accidental loss. Export a copy or restore a valid backup in Protocol.');}
}
function commit(change,message,undo=null){
 let raw,base;try{raw=localStorage.getItem(C.KEY);if(raw===null&&rawCache!==null)throw Error('The saved record was removed in another tab. Reload before making changes.');base=raw!==null?C.validate(JSON.parse(raw)):C.validate(state);}catch(e){storageOK=false;warn('Browser saving is unavailable or saved data changed unexpectedly. This edit was not saved. Export a backup, then reload or restore.');throw Error('Not saved. Browser storage is unavailable or invalid.');}
 const next=C.copy(base);change(next);next.revision=(base.revision||0)+1;const clean=C.validate(next),serialized=JSON.stringify(clean);
 try{localStorage.setItem(C.KEY,serialized);}catch(e){storageOK=false;warn('The browser could not save that edit. The previous record is unchanged. Export a backup and free browser storage before retrying.');throw Error('Not saved. The previous record is unchanged.');}
 state=clean;rawCache=serialized;storageOK=true;warn('');render();toast(message,undo);return true;
}
function error(name,e){text(name+'Error',e.message||String(e));$(name+'Error').hidden=false;}
function resetError(name){$(name+'Error').hidden=true;text(name+'Error','');}
function show(screen){if(!['home','food','progress','more','learn'].includes(screen))return;activeScreen=screen;$$('.screen').forEach(e=>e.hidden=e.id!==screen);$$('.nav [data-screen]').forEach(b=>{if(b.dataset.screen===screen)b.setAttribute('aria-current','page');else b.removeAttribute('aria-current');});if(screen==='food')renderFood();if(screen==='progress')requestAnimationFrame(drawCharts);window.scrollTo({top:0,behavior:'auto'});}
function getTotalsLabel(t,k,unit=''){return C.totalLabel(t,k)+(unit?' '+unit:'');}
function render(){
 const day=C.today(),p=state.profile,m=C.metrics(state,day),t=C.foodTotals(state,day);
 text('todayLabel',new Date(day+'T12:00:00').toLocaleDateString('en-US',{weekday:'long',month:'long',day:'numeric'}));text('currentWeight',pretty(m.current));text('latestDate',C.fmt(m.lastDate));text('weightContext',`Started at ${pretty(p.startWeight)} lb on ${C.fmt(p.startDate,true)}.`);
 text('lostStat',`${pretty(Math.abs(m.lost))} lb`);text('lostLabel',m.lost>=0?'lost since start':'above starting weight');text('goalStat',p.goalWeight===null?'Not set':`${pretty(p.goalWeight)} lb`);
 text('homeCalories',C.totalLabel(t,'calories'));text('homeProtein',C.totalLabel(t,'protein'));text('foodCount',t.count?`${t.count} food ${t.count===1?'entry':'entries'}`:'No entries');
 text('homeFoodNote',t.count?`Today’s entered foods${t.calories.missing||t.protein.missing?' include missing nutrition. Totals may be partial.':'; these totals are based on your entries.'}`:'Your food diary starts with your first meal.');
 const check=state.checkins.find(c=>c.date===day);$$('[data-feeling]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.feeling===check?.feeling)));text('feelingSaved',check?'Saved today':'');
 const meds=state.medications.filter(m=>m.active);$('homeSchedule').innerHTML=meds.length?meds.slice(0,2).map(m=>{const d=C.nextDate(m,state,day);return `<div class="entry"><div class="entry-main"><div class="entry-title">${esc(m.name)}</div><div class="entry-meta">${m.amount} ${esc(m.unit)} · Next slot ${d?C.fmt(d):'not set'}</div></div></div>`;}).join(''):'<p class="small-text">No medication schedule entered yet.</p><p class="tiny" style="margin-top:8px">Your schedule stays separate from other family members.</p>';
 const l=featured(day);text('insightTitle',l.title);text('insightBody',l.body);$('insightLink').href=l.url;text('insightLink',l.source);
 text('progressSubtitle',`${p.startWeight} → ${m.current} lb · ${pretty(Math.abs(m.lost))} lb ${m.lost>=0?'lost':'above start'} since ${C.fmt(p.startDate)}.`);
 text('changeChip',m.weekChange===null?'More history needed':`${pretty(Math.abs(m.weekChange))} lb ${m.weekChange>=0?'change down':'change up'} / ${m.weekSpan} days`);
 text('trendNote',m.avg===null?'Early readings are not used to predict a finish date. The chart shows only your dated weigh-ins.':`Recent mathematical trend: ${pretty(Math.abs(m.avg))} lb/week ${m.avg>=0?'down':'up'}.${m.projected?' Estimated goal date: '+C.fmt(m.projected,true)+'. This is not a promise.':''}`);
 text('goalHeadline',p.goalWeight===null?'Set when you’re ready':`${pretty(p.goalWeight)} lb`);$('goalVisual').hidden=p.goalWeight===null;
 if(m.pct!==null){$('goalFill').style.width=m.pct+'%';$('goalBar').setAttribute('aria-valuenow',m.pct.toFixed(1));text('goalSummary',`${m.pct.toFixed(1)}% of chosen goal · ${m.remain===0?'Goal reached':pretty(m.remain)+' lb remaining'}`);}
 text('goalDateText',m.deadline?`${p.targetDays}-day plan from ${C.fmt(p.startDate)} · Target ${C.fmt(m.deadline,true)} · ${m.remainingDays?m.remainingDays+' days left':m.goalStatus==='Goal reached'?'goal reached':day===m.deadline?'target date is today':'target date passed'}`:'Goal weight and timeframe have not both been set.');
 const hasPlan=p.goalWeight!==null&&p.targetDays!==null;$('comparisonEmpty').hidden=hasPlan;$('comparisonContents').hidden=!hasPlan;
 $('weightHistory').innerHTML=[...state.weights].reverse().map(w=>`<div class="entry"><div><div class="entry-title">${w.weight} lb</div><div class="entry-meta">${C.fmt(w.date,true)}${w.date===p.startDate?' · Starting entry':''}</div></div><div class="actions"><button class="secondary small" data-weight-edit="${w.date}">Edit</button>${w.date===p.startDate?'':`<button class="quiet small danger" data-weight-delete="${w.date}">Remove</button>`}</div></div>`).join('');
 text('settingsSummary',`${p.name} · Started ${p.startWeight} lb on ${C.fmt(p.startDate,true)}. ${p.goalWeight!==null?'Goal '+p.goalWeight+' lb.':'No goal weight set.'} ${p.calorieTarget!==null?'Calorie target: '+p.calorieTarget+'.':'No calorie target set.'} ${p.proteinTarget!==null?'Protein target: '+p.proteinTarget+' g.':'No protein target set.'}`);
 renderFood();renderMedications();renderJourney();if(activeScreen==='progress')requestAnimationFrame(drawCharts);
}
function renderFood(){
 const today=C.today();if(selectedDate>today)selectedDate=today;$('foodDate').value=selectedDate;$('foodDate').max=today;$('foodNext').disabled=selectedDate>=today;
 const t=C.foodTotals(state,selectedDate),p=state.profile;
 $('nutritionTotals').innerHTML=C.NUTRIENTS.map(k=>{const target=k==='calories'?p.calorieTarget:k==='protein'?p.proteinTarget:null,v=t[k],label=k==='calories'?'Calories':`${k[0].toUpperCase()+k.slice(1)} (g)`;return `<div class="nutrition-stat"><b>${C.totalLabel(t,k)}</b><label>${label}</label>${target!==null?`<small>Target ${target}${k==='protein'?' g':''}</small><div class="mini-meter"><span style="width:${C.clamp(v.value/target*100,0,100)}%"></span></div>`:''}<small>${t.count?(v.missing?v.missing+' unknown':t.count+' entered foods'):'No entries'}</small></div>`;}).join('');
 text('foodCoverage',t.count?`${t.count} food ${t.count===1?'entry':'entries'} on ${C.fmt(selectedDate)}. ${t.calories.missing?'Calories are incomplete.':'Calories entered for every logged food.'}`:`No food entries for ${C.fmt(selectedDate)} yet.`);
 $('mealList').innerHTML=['Breakfast','Lunch','Dinner','Snacks'].map((meal,i)=>{
  const rows=state.foods.filter(f=>f.date===selectedDate&&f.meal===meal);
  return `<div class="meal"><div class="meal-header"><div class="meal-heading"><span class="meal-number">0${i+1}</span><h3>${meal}</h3></div><button class="quiet small" data-meal-add="${meal}">Add</button></div>${rows.length?rows.map(f=>`<div class="entry"><div class="entry-main"><div class="entry-title">${esc(f.name)}</div><div class="entry-meta">${f.quantity} × ${esc(f.serving||'serving')}${f.source?' · '+esc(f.source):''}</div><div class="food-totals-inline">${f.calories===null?'Calories not entered':pretty(f.calories*f.quantity)+' cal'} · ${f.protein===null?'Protein not entered':pretty(f.protein*f.quantity)+' g protein'}</div></div><div class="actions"><button class="secondary small" data-food-edit="${f.id}">Edit</button><button class="quiet small danger" data-food-delete="${f.id}">Remove</button></div></div>`).join(''):'<p class="empty-text">Nothing logged yet.</p>'}</div>`;
 }).join('');
 const seen=new Set(),recent=[...state.foods].reverse().filter(f=>{const key=[f.name,f.serving,f.calories,f.protein,f.carbs,f.fat].join('|');if(seen.has(key))return false;seen.add(key);return true;}).slice(0,8);
 $('recentFoods').innerHTML=recent.length?recent.map(f=>`<button data-food-repeat="${f.id}">${esc(f.name)}</button>`).join(''):'<p class="small-text">Your recent foods will appear after you start logging.</p>';
 $('foodWeek').innerHTML=Array.from({length:7},(_,i)=>C.addDays(selectedDate,i-6)).map(d=>{const v=C.foodTotals(state,d);return `<button class="week-day" data-food-day="${d}" aria-label="Food diary ${C.fmt(d,true)}"><small>${C.fmt(d)}</small><strong>${C.totalLabel(v,'calories')}</strong></button>`;}).join('');
}
function renderMedications(){
 const day=C.today();$('medList').innerHTML=state.medications.length?state.medications.map(m=>{
  const due=C.dueDate(m,day),last=due?state.doses.find(d=>d.medId===m.id&&d.scheduledDate===due):null,next=C.nextDate(m,state,day);
  return `<div class="meal"><div class="med-header"><div class="entry-main"><div class="entry-title">${esc(m.name)}${m.active?'':' · Paused'}</div><div class="entry-meta">${m.amount} ${esc(m.unit)} · ${m.days.map(d=>DAYS[d]).join(', ')}</div><div class="entry-meta">${m.active?'Next calendar slot: '+(next?C.fmt(next):'not set'):'History retained while paused.'}</div><div class="entry-meta">${due?C.fmt(due)+': '+(last?last.status:'not logged'):'First scheduled date has not arrived.'}</div></div><button class="quiet small" data-med-edit="${m.id}">Edit</button></div><div class="actions section">${m.active?`<button class="blue small" data-dose-open="${m.id}"${!due?' disabled':''}>${last?'View / correct log':'Record what happened'}</button>`:''}<button class="secondary small" data-med-pause="${m.id}">${m.active?'Pause schedule':'Resume schedule'}</button></div></div>`;
 }).join(''):'<p class="small-text">No medications copied from another profile. Add Ericka’s confirmed schedule when ready.</p>';
 $('doseHistory').innerHTML=state.doses.length?[...state.doses].sort((a,b)=>b.scheduledDate.localeCompare(a.scheduledDate)).map(d=>`<div class="entry"><div class="entry-main"><div class="entry-title">${esc(d.name)} · ${d.amount} ${esc(d.unit)}</div><div class="entry-meta">Scheduled ${C.fmt(d.scheduledDate,true)} · ${d.status==='taken'?'Taken '+C.fmt(d.actualDate):'Skipped'}</div></div><div class="actions"><button class="secondary small" data-dose-edit="${d.id}">Edit</button><button class="quiet small danger" data-dose-delete="${d.id}">Remove</button></div></div>`).join(''):'<p class="small-text">No doses recorded yet.</p>';
}
function openFood({id=null,repeat=null,meal=null}={}){
 $('foodForm').reset();resetError('food');const f=id||repeat?state.foods.find(x=>x.id===(id||repeat)):null;
 text('foodDialogTitle',id?'Edit food':'Add food');$('foodId').value=id||'';$('entryDate').value=id?f.date:activeScreen==='food'?selectedDate:C.today();$('entryDate').max=C.today();$('mealInput').value=meal||f?.meal||'Breakfast';$('foodName').value=f?.name||'';$('servingInput').value=f?.serving||'';$('quantityInput').value=f?.quantity??1;
 for(const k of C.NUTRIENTS)$(k+'Input').value=f?.[k]??'';$('sourceInput').value=f?.source||'';previewFood();$('foodDialog').showModal();setTimeout(()=>$('foodName').focus(),0);
}
function previewFood(){
 const q=Number($('quantityInput').value);let a=[];for(const k of ['calories','protein']){const raw=$(k+'Input').value;if(raw.trim()&&Number.isFinite(q)&&q>0){a.push(`${pretty(Number(raw)*q)} ${k==='calories'?'calories':'g protein'}`);}}
 text('portionPreview',a.length?'For this portion: '+a.join(' · '):'Enter per-serving nutrition to preview this portion’s totals.');
}
function saveFood(e){e.preventDefault();resetError('food');try{
 const editing=$('foodId').value,old=editing?state.foods.find(f=>f.id===editing):null;
 const f={id:editing||uid(),date:$('entryDate').value,meal:$('mealInput').value,name:$('foodName').value,serving:$('servingInput').value,quantity:C.num($('quantityInput').value,'Servings eaten',0.01,1000),source:$('sourceInput').value};
 for(const k of C.NUTRIENTS)f[k]=C.num($(k+'Input').value,k,0,k==='calories'?50000:10000,true);
 commit(s=>{if(editing&&!s.foods.some(x=>x.id===editing))throw Error('This record changed in another tab. Reopen the diary.');s.foods=s.foods.filter(x=>x.id!==f.id);s.foods.push(f);},editing?'Food updated.':'Food saved.',()=>commit(s=>{s.foods=s.foods.filter(x=>x.id!==f.id);if(old)s.foods.push(old);},'Food change undone.'));
 selectedDate=f.date;renderFood();$('foodDialog').close();
 }catch(err){error('food',err);}}
function openWeight(day=null){resetError('weight');const m=C.metrics(state),w=day?state.weights.find(x=>x.date===day):null;$('weightInput').value=w?.weight??m.current;$('weightDate').value=day||C.today();$('weightDate').min=state.profile.startDate;$('weightDate').max=C.today();$('weightDialog').showModal();}
function saveWeight(e){e.preventDefault();resetError('weight');try{const date=$('weightDate').value,weight=C.num($('weightInput').value,'Weight',0.1,1500),old=state.weights.find(w=>w.date===date),oldStart=state.profile.startWeight;commit(s=>C.upsertWeight(s,{date,weight}),old?'Weight updated.':'Weight saved.',()=>commit(s=>{s.weights=s.weights.filter(w=>w.date!==date);if(old)s.weights.push(old);if(date===s.profile.startDate)s.profile.startWeight=oldStart;},'Weight change undone.'));$('weightDialog').close();}catch(err){error('weight',err);}}
function openPlan(){resetError('plan');const p=state.profile;$('startWeightInput').value=p.startWeight;$('startDateInput').value=p.startDate;$('startDateInput').max=C.today();$('goalInput').value=p.goalWeight??'';$('targetDateInput').value=p.targetDate||(p.targetDays?C.addDays(p.startDate,p.targetDays):'');$('targetDateInput').min=C.addDays(p.startDate,1);$('calorieTargetInput').value=p.calorieTarget??'';$('proteinTargetInput').value=p.proteinTarget??'';$('planDialog').showModal();}
function savePlan(e){e.preventDefault();resetError('plan');try{
 const profile={name:'Ericka',startWeight:C.round(C.num($('startWeightInput').value,'Starting weight',0.1,1500)),startDate:$('startDateInput').value,goalWeight:C.num($('goalInput').value,'Goal weight',0.1,1500,true),targetDays:$('targetDateInput').value?C.ord($('targetDateInput').value)-C.ord($('startDateInput').value):null,targetDate:$('targetDateInput').value||null,planRevision:2,calorieTarget:C.num($('calorieTargetInput').value,'Calorie target',1,15000,true),proteinTarget:C.num($('proteinTargetInput').value,'Protein target',0.1,1000,true)};
 commit(s=>{const oldDate=s.profile.startDate;if(profile.startDate!==oldDate&&s.weights.some(w=>w.date===profile.startDate))throw Error('That starting date already has a weigh-in. Choose the correct baseline or edit that record first.');s.weights=s.weights.filter(w=>w.date!==oldDate);s.weights.push({date:profile.startDate,weight:profile.startWeight});s.profile=profile;},'Details updated.');$('planDialog').close();
 }catch(err){error('plan',err);}}
function openMed(id=null){resetError('med');$('medForm').reset();const m=id?state.medications.find(m=>m.id===id):null;text('medDialogTitle',m?'Edit schedule':'Add medication');$('medId').value=id||'';$('medName').value=m?.name||'';$('medAmount').value=m?.amount??'';$('medUnit').value=m?.unit||'mg';$('medStart').value=m?.startDate||C.today();$('medStart').max=C.today();$('medDays').innerHTML=DAYS.map((d,i)=>`<label><input type="checkbox" value="${i}"${m?.days.includes(i)?' checked':''}>${d}</label>`).join('');$('medDialog').showModal();}
function saveMed(e){e.preventDefault();resetError('med');try{
 const old=state.medications.find(m=>m.id===$('medId').value),m={id:$('medId').value||uid(),name:$('medName').value,amount:C.num($('medAmount').value,'Dose amount',0.0001,100000),unit:$('medUnit').value,startDate:$('medStart').value,days:$$('#medDays input:checked').map(c=>Number(c.value)),active:old?.active??true};
 commit(s=>{s.medications=s.medications.filter(x=>x.id!==m.id);s.medications.push(m);},'Medication schedule saved.');$('medDialog').close();
 }catch(err){error('med',err);}}
function openDose(medId,editId=null){
 resetError('dose');const med=state.medications.find(m=>m.id===medId);if(!med)return;
 let old=editId?state.doses.find(d=>d.id===editId):null;const due=C.dueDate(med,C.today());if(!old&&due)old=state.doses.find(d=>d.medId===medId&&d.scheduledDate===due);
 if(!old&&!due){toast('No scheduled date has arrived yet.');return;}
 $('doseId').value=old?.id||'';$('doseMedId').value=medId;$('scheduledDate').value=old?.scheduledDate||due;$('scheduledDate').max=C.today();$('scheduledDate').min=old?'1900-01-01':med.startDate;$('actualDate').value=old?.actualDate||C.today();$('actualDate').max=C.today();$('doseStatus').value=old?.status||'taken';text('doseNameText',`${old?.name||med.name} · ${old?.amount??med.amount} ${old?.unit||med.unit}${old?' · Dose stored when this log was created':''}`);toggleDose();$('doseDialog').showModal();
}
function toggleDose(){const skipped=$('doseStatus').value==='skipped';$('actualDateField').hidden=skipped;$('actualDate').required=!skipped;}
function saveDose(e){e.preventDefault();resetError('dose');try{
 const d={id:$('doseId').value||uid(),medId:$('doseMedId').value,scheduledDate:$('scheduledDate').value,actualDate:$('doseStatus').value==='taken'?$('actualDate').value:null,status:$('doseStatus').value},old=state.doses.find(x=>x.id===d.id);
 commit(s=>C.upsertDose(s,d),'Dose log saved.',()=>commit(s=>{s.doses=s.doses.filter(x=>x.id!==d.id);if(old)s.doses.push(old);},'Dose change undone.'));$('doseDialog').close();
 }catch(err){error('dose',err);}}
function removeFood(id){const f=state.foods.find(f=>f.id===id);if(!f)return;commit(s=>{s.foods=s.foods.filter(f=>f.id!==id);},'Food removed.',()=>commit(s=>{if(s.foods.some(x=>x.id===id))throw Error('Already restored.');s.foods.push(f);},'Food restored.'));}
function removeWeight(day){const w=state.weights.find(w=>w.date===day);if(!w)return;if(day===state.profile.startDate)throw Error('Edit the starting entry in Details instead.');commit(s=>{s.weights=s.weights.filter(w=>w.date!==day);},'Weight removed.',()=>commit(s=>{if(s.weights.some(x=>x.date===day))throw Error('A new entry exists on that date.');s.weights.push(w);},'Weight restored.'));}
function removeDose(id){const d=state.doses.find(d=>d.id===id);if(!d)return;commit(s=>{s.doses=s.doses.filter(d=>d.id!==id);},'Dose log removed.',()=>commit(s=>{s.doses.push(d);},'Dose log restored.'));}
function recordsAnswer(q){
 q=q.toLowerCase().trim();const t=C.foodTotals(state,C.today()),m=C.metrics(state),p=state.profile;
 if(/symptom|side effect|vomit|chest pain|faint|dose increase|how much.*take|should.*take/.test(q))return 'This tracker cannot assess symptoms or recommend treatment or dose changes. Contact your clinician. Severe or urgent symptoms need prompt medical care.';
 if(/protein|calorie|food|carb|fat|eat|eaten/.test(q)){if(!t.count)return 'No foods are logged today. Add an item in Food to start today’s totals.';return `Today: ${getTotalsLabel(t,'calories','calories')} and ${getTotalsLabel(t,'protein','g protein')} from ${t.count} food entries. Carbs: ${getTotalsLabel(t,'carbs','g')}; fat: ${getTotalsLabel(t,'fat','g')}. ${C.NUTRIENTS.some(k=>t[k].missing)?'Some nutrition is unknown; a plus sign means that total is partial.':''} These are logged amounts, not an assessment of your full day.`;}
 if(/weight|lost|loss|progress/.test(q))return `Starting weight: ${p.startWeight} lb on ${C.fmt(p.startDate,true)}. Latest: ${m.current} lb on ${C.fmt(m.lastDate,true)}. Change: ${pretty(Math.abs(m.lost))} lb ${m.lost>=0?'down':'up'}. ${m.projected?'A mathematical trend estimate is '+C.fmt(m.projected,true)+', not a guaranteed finish date.':'No finish date is predicted from these early readings.'}`;
 if(/medication|taking|schedule|dose|shot|peptide/.test(q)){const meds=state.medications.filter(m=>m.active);return meds.length?meds.map(m=>`${m.name}: ${m.amount} ${m.unit}; ${m.days.map(d=>DAYS[d]).join(', ')}. Next calendar slot: ${C.fmt(C.nextDate(m,state))}.`).join('\n')+'\nThis is your recorded schedule, not an instruction to take a dose.':'No medication schedule is saved for Ericka. Add her confirmed schedule in Protocol; nothing is copied from other family members.';}
 if(/goal|target/.test(q))return `${p.goalWeight===null?'No goal weight is set.':'Your chosen goal is '+p.goalWeight+' lb.'} ${m.deadline?'Target date: '+C.fmt(m.deadline,true)+'.':'No target date is set.'} Change your own tracking targets in Protocol.`;
 if(/start/.test(q))return `Your starting weigh-in is ${p.startWeight} lb on ${C.fmt(p.startDate,true)}.`;
 return 'I can summarize your saved weight, food totals, goal, or medication schedule. This is the local records / sourced-knowledge mode. Use Ask AI for model chat when the connection is available. Unknown nutrition is never guessed.';
}
function answerQuestion(q){
 const query=String(q).trim();
 if(/\b(my goal|my target|goal date|target date|when.*goal)\b/i.test(query)){const m=C.metrics(state);return 'Your chosen goal is '+state.profile.goalWeight+' lb by '+C.fmt(m.deadline,true)+'. '+m.remain+' lb remains from your latest recorded weight. This is your tracking goal, not a medical recommendation.';}
 if(/\b(symptom|vomit|pain|side effect|pregnan|breastfeed|dizzy|faint|dose should|how much should|increase|double|combine)\b/i.test(query))return 'For symptoms, pregnancy, breastfeeding, dose changes or combining treatments, contact your clinician or pharmacist. This app logs and explains; it does not prescribe.';
 const hits=findKnowledge(query);
 if(hits.length && !/\b(my|today|taking|next|logged)\b/i.test(query))return hits.slice(0,2).map(k=>k.name+' — '+k.level+'\n'+k.body+'\n'+k.risk+'\nSource: '+k.source+'\n'+k.url+'\nReviewed '+k.reviewed).join('\n\n');
 return recordsAnswer(query);
}

function ask(q){if(!q.trim())return;text('answer',answerQuestion(q));$('answer').hidden=false;}

function renderJourney(){
 const m=C.metrics(state),p=state.profile;
 text('journeyGoal',p.goalWeight===null?'Goal not set':p.goalWeight+' lb');
 text('journeyPct',m.pct===null?'—':m.pct.toFixed(1)+'%');
 $('journeyRing').style.setProperty('--turn',(m.pct||0)*3.6+'deg');
 text('journeyRemaining',m.remain===null?'Set a goal':pretty(m.remain)+' lb remaining');
 text('journeyDate',m.deadline?C.fmt(m.deadline,true):'Choose a target date');
 text('journeyDays',m.remainingDays===null?'—':m.remainingDays+' days left');
 text('plannedAverage',m.planPace===null?'—':m.planPace.toFixed(2)+' lb/week');
 text('remainingAverage',m.remainingPace===null?(m.remain===0?'Goal reached':'—'):m.remainingPace.toFixed(2)+' lb/week');
 text('actualAverage',m.avg===null?'More check-ins needed':Math.abs(m.avg).toFixed(2)+' lb/week '+(m.avg>=0?'down':'up'));
 text('projectedDate',m.remain===0?'Goal reached':m.projected?C.fmt(m.projected,true):'No estimate yet');
 text('paceStatus',m.paceStatus);
 const goal=p.goalWeight,start=p.startWeight;
 const levels=goal===null?[]:[start,...[150,140,130,120,110].filter(w=>w<start&&w>goal),goal];
 $('milestones').innerHTML=levels.map((w,i)=>`<div class="milestone ${m.current<=w?'reached':''}"><b>${w}</b><small>${i===0?'Start':i===levels.length-1?'Goal':m.current<=w?'Reached':'Milestone'}</small></div>`).join('');
}
function renderKnowledge(query=''){
 const q=String(query).toLowerCase();
 const items=KNOWLEDGE.filter(k=>(k.name+' '+k.category+' '+k.aliases.join(' ')).toLowerCase().includes(q));
 $('knowledgeList').innerHTML=items.length?items.map(k=>`<details class="knowledge-item"><summary>${esc(k.name)} <small>${esc(k.level)}</small></summary><p>${esc(k.body)}</p><p class="small-text">${esc(k.risk)}</p><a href="${esc(k.url)}" target="_blank" rel="noopener noreferrer">${esc(k.source)}</a><p class="tiny">Reviewed ${k.reviewed} · Education, not a treatment recommendation.</p></details>`).join(''):'<p>No reviewed entry matches. The library does not invent a treatment summary.</p>';
}
function aiPayload(question){
 const m=C.metrics(state),t=C.foodTotals(state,C.today());
 return {question,knowledge:findKnowledge(question).slice(0,3),...( $('shareSummary').checked?{summary:{date:C.today(),startWeight:state.profile.startWeight,startDate:state.profile.startDate,currentWeight:m.current,currentWeightDate:m.lastDate,goalWeight:state.profile.goalWeight,targetDate:m.deadline,poundsLost:m.lost,remaining:m.remain,foods:t,medications:state.medications.filter(v=>v.active).map(({name,amount,unit,days})=>({name,amount,unit,days}))}}:{})};
}
async function askLive(){
 const question=$('question').value.trim();if(!question){toast('Enter a question first.');return;}
 if(!confirm('Send this question'+($('shareSummary').checked?' and your selected tracker summary':'')+' to the connected AI service? No records are changed.'))return;
 const b=$('liveAI');b.disabled=true;text('aiStatus','Checking AI connection…');
 try{
  const response=await fetch('./api/ask',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(aiPayload(question)),signal:AbortSignal.timeout(25000)});
  if(!response.ok)throw Error('AI is not connected or is temporarily unavailable.');
  const data=await response.json();if(typeof data.answer!=='string'||!data.answer.trim())throw Error('The AI service returned no answer.');
  text('answer',data.answer);$('answer').hidden=false;text('aiStatus','AI-generated answer · Check medical details with your clinician.');
 }catch(e){text('aiStatus','Live AI is not connected on this version. The peptide library and saved-record answers still work.');toast('No AI answer was generated. Your records are unchanged.');}
 finally{b.disabled=false;}
}

function download(name,text,type){const u=URL.createObjectURL(new Blob([text],{type})),a=document.createElement('a');a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),60000);}
function exportBackup(){if(!storageOK&&rawCache!==null){download(`ericka-recovery-${C.today()}.json`,rawCache,'application/json');toast('Original stored data exported for recovery. It may need repair before restoring.');return;}download(`ericka-backup-${C.today()}.json`,JSON.stringify({format:'ericka-journal',version:1,exportedAt:new Date().toISOString(),data:state},null,2),'application/json');toast('Backup exported. Keep this personal file private.');}
function csvValue(x){let s=String(x??'');if(/^[=+@\-\t\r]/.test(s))s="'"+s;return '"'+s.replace(/"/g,'""')+'"';}
function foodCSV(){const cols=['Date','Meal','Food','Serving description','Servings eaten','Calories per serving','Protein g per serving','Carbs g per serving','Fat g per serving','Calories eaten','Protein g eaten','Carbs g eaten','Fat g eaten','Nutrition source'];return [cols,...[...state.foods].sort((a,b)=>a.date.localeCompare(b.date)).map(f=>[f.date,f.meal,f.name,f.serving,f.quantity,...C.NUTRIENTS.map(k=>f[k]),...C.NUTRIENTS.map(k=>f[k]===null?'':C.round(f[k]*f.quantity)),f.source])].map(r=>r.map(csvValue).join(',')).join('\r\n');}
async function restore(e){const file=e.target.files[0];if(!file)return;try{if(file.size>5000000)throw Error('Backup is too large.');const raw=JSON.parse(await file.text()),clean=C.validate(raw.data||raw);if(!confirm('Replace Ericka’s records in this browser with this backup? Export the current records first if needed.'))return;const old=localStorage.getItem(C.KEY);if(old)localStorage.setItem(C.KEY+'-before-restore-'+Date.now(),old);localStorage.setItem(C.KEY,JSON.stringify(clean));state=clean;rawCache=JSON.stringify(clean);storageOK=true;warn('');render();toast('Ericka’s backup restored.');}catch(err){toast('Backup not restored: '+err.message);}finally{e.target.value='';}}
function chartBase(el,lo,hi,labels){
 const W=Math.max(260,el.parentElement.clientWidth),H=265,L=42,R=W-18,T=24,B=220;el.setAttribute('viewBox',`0 0 ${W} ${H}`);const y=v=>B-(v-lo)/(hi-lo||1)*(B-T),x=t=>L+t*(R-L);let out='';
 for(let i=0;i<=4;i++){const v=lo+(hi-lo)*i/4,yy=y(v);out+=`<line x1="${L}" x2="${R}" y1="${yy}" y2="${yy}" stroke="#dcc3d0" stroke-width="1"/><text x="${L-7}" y="${yy+4}" text-anchor="end" fill="#735a6a" font-size="11">${pretty(v)}</text>`;}
 for(const [t,l] of labels)out+=`<text x="${x(t)}" y="${B+25}" text-anchor="${t===0?'start':t===1?'end':'middle'}" fill="#735a6a" font-size="11">${esc(l)}</text>`;
 return {x,y,L,R,T,B,out};
}
function drawCharts(){if(activeScreen!=='progress')return;const m=C.metrics(state),p=state.profile,pts=m.points;
 if(pts.length){const min=Math.min(...pts.map(p=>p.weight)),max=Math.max(...pts.map(p=>p.weight)),a=C.ord(pts[0].date),b=C.ord(pts.at(-1).date),g=chartBase($('actualChart'),Math.floor(min-2),Math.ceil(max+2),[[0,C.fmt(pts[0].date)],[1,C.fmt(pts.at(-1).date)]]),xx=p=>g.x(a===b?.5:(C.ord(p.date)-a)/(b-a));
  $('actualChart').innerHTML=g.out+(pts.length>1?`<path id="recordedLine" d="${pts.map((p,i)=>(i?'L':'M')+xx(p)+' '+g.y(p.weight)).join(' ')}" stroke="#9f2f65" fill="none" stroke-width="3.5" stroke-linecap="round"/>`:'')+pts.map((p,i)=>`<circle cx="${xx(p)}" cy="${g.y(p.weight)}" r="5" stroke="#9f2f65" stroke-width="2.5" fill="#fff0f6"><title>${C.fmt(p.date,true)}: ${p.weight} lb</title></circle>${i===pts.length-1?`<text x="${xx(p)}" y="${g.y(p.weight)-13}" text-anchor="${a===b?'middle':'end'}" font-size="12" font-weight="700" fill="#692044">${p.weight} lb</text>`:''}`).join('');
 }
 if(p.goalWeight===null||p.targetDays===null)return;
 const goal=p.goalWeight,days=p.targetDays,base=p.startWeight,a=C.ord(p.startDate),extent=Math.max(days,...pts.map(w=>C.ord(w.date)-a)),g=chartBase($('comparisonChart'),Math.floor(Math.min(goal,...pts.map(p=>p.weight))-2),Math.ceil(Math.max(base,...pts.map(p=>p.weight))+2),[[0,'Day 0'],[.5,'Day '+Math.round(extent/2)],[1,'Day '+extent]]),x=d=>g.x(d/extent);
 let out=g.out+`<path id="goalLine" d="M${x(0)} ${g.y(base)} L${x(days)} ${g.y(goal)}" stroke="#235975" stroke-width="3" fill="none"><title>Chosen ${days}-day goal</title></path>`;
 [[1,'#89778c','8 5'],[2,'#5f8fa7','2 5']].forEach(([r,col,dash])=>{const d=Math.min(extent,(base-goal)*7/r);out+=`<path data-rate="${r}" d="M${x(0)} ${g.y(base)} L${x(d)} ${g.y(base-r*d/7)}" stroke="${col}" stroke-width="2.5" fill="none" stroke-dasharray="${dash}"><title>${r} lb/week general reference</title></path>`;});
 out+=`<path id="actualComparisonLine" d="${pts.map((w,i)=>(i?'L':'M')+x(C.ord(w.date)-a)+' '+g.y(w.weight)).join(' ')}" stroke="#9f2f65" stroke-width="4" fill="none"/>`;
 pts.forEach(w=>{out+=`<circle cx="${x(C.ord(w.date)-a)}" cy="${g.y(w.weight)}" r="4" stroke="#9f2f65" stroke-width="2" fill="#fff4f8"><title>${C.fmt(w.date,true)}: ${w.weight} lb</title></circle>`;});$('comparisonChart').innerHTML=out;
}
function click(e){const b=e.target.closest('button');if(!b)return;const d=b.dataset;try{
 if(d.screen){show(d.screen);return;}if(d.close){$(d.close).close();return;}if(d.open){({food:openFood,weight:openWeight,plan:openPlan})[d.open]?.();return;}
 if(d.feeling){commit(s=>{s.checkins=s.checkins.filter(c=>c.date!==C.today());s.checkins.push({date:C.today(),feeling:d.feeling});},'Today’s check-in saved.');return;}
 if(d.question){$('question').value=d.question;ask(d.question);return;}if(d.mealAdd){openFood({meal:d.mealAdd});return;}if(d.foodEdit){openFood({id:d.foodEdit});return;}if(d.foodRepeat){openFood({repeat:d.foodRepeat});return;}if(d.foodDelete){removeFood(d.foodDelete);return;}if(d.foodDay){selectedDate=d.foodDay;renderFood();return;}
 if(d.weightEdit){openWeight(d.weightEdit);return;}if(d.weightDelete){removeWeight(d.weightDelete);return;}
 if(d.medEdit){openMed(d.medEdit);return;}if(d.medPause){commit(s=>{const m=s.medications.find(m=>m.id===d.medPause);m.active=!m.active;},'Schedule updated.');return;}
 if(d.doseOpen){openDose(d.doseOpen);return;}if(d.doseEdit){const v=state.doses.find(v=>v.id===d.doseEdit);openDose(v.medId,v.id);return;}if(d.doseDelete){removeDose(d.doseDelete);return;}
 }catch(err){toast(err.message);}}
function connect(){
 document.addEventListener('click',click);$('foodForm').onsubmit=saveFood;$('weightForm').onsubmit=saveWeight;$('planForm').onsubmit=savePlan;$('medForm').onsubmit=saveMed;$('doseForm').onsubmit=saveDose;$('doseStatus').onchange=toggleDose;$('addMedBtn').onclick=()=>openMed();
 ['quantityInput','caloriesInput','proteinInput'].forEach(id=>$(id).oninput=previewFood);
 $('foodDate').onchange=()=>{try{C.date($('foodDate').value);selectedDate=$('foodDate').value;renderFood();}catch(e){$('foodDate').value=selectedDate;toast(e.message);}};
 $('foodPrev').onclick=()=>{selectedDate=C.addDays(selectedDate,-1);renderFood();};$('foodNext').onclick=()=>{if(selectedDate<C.today()){selectedDate=C.addDays(selectedDate,1);renderFood();}};$('foodToday').onclick=()=>{selectedDate=C.today();renderFood();};
 $('helperForm').onsubmit=e=>{e.preventDefault();ask($('question').value);};$('exportBtn').onclick=exportBackup;$('csvBtn').onclick=()=>{download('ericka-food-diary-'+C.today()+'.csv',foodCSV(),'text/csv;charset=utf-8');toast('Food diary CSV exported.');};$('importBtn').onclick=()=>$('importFile').click();$('importFile').onchange=restore;
 window.addEventListener('resize',()=>requestAnimationFrame(drawCharts));window.addEventListener('storage',e=>{if(e.key===C.KEY){if(e.newValue===null){storageOK=false;warn('Records were cleared in another tab. Reload or restore a backup before editing.');}else{load();render();}}});
 const refresh=()=>{const d=C.today();if(d!==currentDay){if(selectedDate===currentDay)selectedDate=d;currentDay=d;render();}};window.addEventListener('focus',refresh);document.addEventListener('visibilitychange',()=>{if(!document.hidden)refresh();});setInterval(refresh,30000);
}
load();connect();render();renderKnowledge();$('knowledgeSearch').oninput=()=>renderKnowledge($('knowledgeSearch').value);$('liveAI').onclick=askLive;$('fileWarning').hidden=location.protocol!=='file:';
window.ErickaApp={version:'2.0.0',getState:()=>C.copy(state),getMetrics:()=>C.metrics(state),getTotals:d=>C.foodTotals(state,d||C.today()),answerQuestion,featured,show,foodCSV,getKnowledge:()=>C.copy(KNOWLEDGE),findKnowledge,aiPayload};
})();

